## Packages
framer-motion | Smooth animations and page transitions
recharts | Dashboard analytics and visualizations
date-fns | Date formatting for tasks and projects

## Notes
Authentication uses session-based auth with httpOnly cookies via `credentials: "include"`.
Dashboard requires fetching user organization details.
Projects have tasks which can be viewed in a list or board format.
Sidebar navigation structure: Dashboard, Projects, Tasks, Team, Settings.
